ALTER TABLE public.tenant_integrations ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS "Allow super_admin to manage global integrations" ON public.tenant_integrations;

CREATE POLICY "Allow super_admin to manage global integrations" ON public.tenant_integrations FOR ALL
    USING ( (auth.jwt() ->> 'role') = 'super_admin' );
